---
import SiteLayout from "../layouts/SiteLayout.astro";
import ContentRow from "../features/listing/ContentRow.astro";
import ArtBand from "../components/ArtBand.astro";
import Tate from "../components/Tate.astro";
import KindSection from "../features/listing/KindSection.astro";
import About from "../features/about/About.astro";
import AboutLinks from "../features/about/AboutLinks.astro";
import OutsideList from "../features/about/OutsideList.astro";
import { listContent } from "../lib/content";

const [all, labs, articles] = await Promise.all([
  listContent(),
  listContent({ type: "hands-on" }),
  listContent({ type: "article" }),
]);
---

<SiteLayout title="fukuemon.dev" description="読んで、動かして、確かめる。">
  <About />

  <section data-band class="py-inter-2 px-(--gutter) border-y border-rule">
    <div class="g-rail">
      <Tate as="h2" class="tate-head text-fg" text="最近の更新" />
      <div class="flex justify-end items-baseline pb-intra-1">
        <a class="mono meta" href="/blog">すべて見る →</a>
      </div>
      <div>
        {all.slice(0, 6).map((r, i) => <ContentRow ref={r} trailing="kind" cap={false} index={i} />)}
      </div>
    </div>
  </section>

  <KindSection
    heading="ハンズオン"
    items={labs}
    lead="ハンズオン形式で整理するコンテンツ Try X in Y minutes に影響を受けてます"
  />

  <KindSection heading="記事" items={articles} lead="調べたことや思ったことをつらつらと" />

  <OutsideList />

  <section class="section--flush about__foot mx-(--gutter)">
    <AboutLinks />
  </section>

  <ArtBand motif="grass" height="200px" fade />
</SiteLayout>
